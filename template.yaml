AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: |
  sam-app
  Sample SAM Template for sam-app

Metadata:
  TemplateConfig:
    x-db-function-properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
# More info about Globals: https://github.com/awslabs/serverless-application-model/blob/master/docs/globals.rst
Globals:
  Function:
    Timeout: 3
  Api:
    # This configures API Gateway to answer the browser's "OPTIONS" check
    Cors:
      AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS,PATCH'"
      AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
      AllowOrigin: "'*'" # <--- Note the quoting: "'*'"

Parameters:
  CognitoUserPoolArn:
    Type: String
    Description: The ARN of the pre-existing Cognito User Pool
  DbName:
    Type: String
    Description: The database name to connect to
    Default: postgres
  LambdaSecurityGroupId:
    Type: AWS::EC2::SecurityGroup::Id
    Description: The Security Group to attach to the Lambda functions
  PrivateSubnetOneId:
    Type: AWS::EC2::Subnet::Id
    Description: A private subnet ID for the lambda
  PrivateSubnetTwoId:
    Type: AWS::EC2::Subnet::Id
    Description: A private subnet ID for the lambda
  PrivateSubnetThreeId:
    Type: AWS::EC2::Subnet::Id
    Description: A private subnet ID for the lambda
  PrivateSubnetFourId:
    Type: AWS::EC2::Subnet::Id
    Description: A private subnet ID for the lambda
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: The VPC ID for creating VPC endpoints
Resources:
  # ----------------------------------------------------------
  # VPC Endpoint for SQS (so VPC Lambdas can reach SQS)
  # ----------------------------------------------------------
  SqsVpcEndpoint:
    Type: AWS::EC2::VPCEndpoint
    Properties:
      VpcId: !Ref VpcId
      ServiceName: !Sub com.amazonaws.${AWS::Region}.sqs
      VpcEndpointType: Interface
      PrivateDnsEnabled: true
      SubnetIds:
        - !Ref PrivateSubnetOneId
      SecurityGroupIds:
        - !Ref LambdaSecurityGroupId

  # ----------------------------------------------------------
  # AppSync Events API (real-time pub/sub for match scoring)
  # ----------------------------------------------------------
  MatchEventsApi:
    Type: AWS::AppSync::Api
    Properties:
      Name: match-events-api
      EventConfig:
        AuthProviders:
          - AuthType: AWS_IAM
          - AuthType: AMAZON_COGNITO_USER_POOLS
            CognitoConfig:
              UserPoolId: 'us-west-2_4xTIIoc8k'
              AwsRegion: us-west-2
        ConnectionAuthModes:
          - AuthType: AMAZON_COGNITO_USER_POOLS
        DefaultPublishAuthModes:
          - AuthType: AWS_IAM
        DefaultSubscribeAuthModes:
          - AuthType: AMAZON_COGNITO_USER_POOLS

  MatchEventsChannelNamespace:
    Type: AWS::AppSync::ChannelNamespace
    Properties:
      ApiId: !GetAtt MatchEventsApi.ApiId
      Name: default

  RdsCertLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: RdsCertLayer
      ContentUri: layers/certs/
      CompatibleRuntimes:
        - nodejs22.x

  SharedUtilsLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: SharedUtilsLayer
      ContentUri: layers/shared/nodejs/
      CompatibleRuntimes:
        - nodejs22.x
    Metadata:
      BuildMethod: nodejs22.x
      BuildArchitecture: x86_64

  MyApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      Cors:
        AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS,PATCH'"
        AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
        AllowOrigin: "'*'" # <--- Note the quoting: "'*'"
      Auth:
        Authorizers:
          MyCognitoAuthorizer:
            Type: COGNITO_USER_POOLS
            UserPoolArn: !Ref CognitoUserPoolArn
        DefaultAuthorizer: MyCognitoAuthorizer
        AddDefaultAuthorizerToCorsPreflight: false
      GatewayResponses:
        DEFAULT_4XX:
          ResponseParameters:
            Headers:
              Access-Control-Allow-Origin: "'*'"
              Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
              Access-Control-Allow-Methods: "'GET,POST,PUT,DELETE,OPTIONS,PATCH'"
        DEFAULT_5XX:
          ResponseParameters:
            Headers:
              Access-Control-Allow-Origin: "'*'"
              Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
              Access-Control-Allow-Methods: "'GET,POST,PUT,DELETE,OPTIONS,PATCH'"

  DraftAPI:
    Type: AWS::Serverless::GraphQLApi
    Properties:
      SchemaUri: draft-functions/schema.graphql
      Auth:
        Type: AMAZON_COGNITO_USER_POOLS
        UserPool:
          UserPoolId: 'us-west-2_4xTIIoc8k'
          AwsRegion: us-west-2
          DefaultAction: ALLOW
        Additional:
          - Type: AWS_IAM
      DataSources:
        Lambda:
          DraftDataSource:
            FunctionArn: !GetAtt DraftFunction.Arn
          DraftSchedulerDataSource:
            FunctionArn: !GetAtt DraftSchedulerFunction.Arn
      Functions:
        getDraftPicksFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftDataSource
          CodeUri: ./draft-functions/resolvers/getDraftPicks.js
        getDraftStateFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftDataSource
          CodeUri: ./draft-functions/resolvers/getDraftState.js
        postDraftPickFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftDataSource
          CodeUri: ./draft-functions/resolvers/postDraftPick.js
        startDraftFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftDataSource
          CodeUri: ./draft-functions/resolvers/startDraft.js
        autoPickDraftPlayerFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftDataSource
          CodeUri: ./draft-functions/resolvers/autoPickDraftPlayer.js
        endDraftFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftDataSource
          CodeUri: ./draft-functions/resolvers/endDraft.js
        schedulePickFunction:
          Runtime:
            Name: APPSYNC_JS
            Version: 1.0.0
          DataSource: DraftSchedulerDataSource
          CodeUri: ./draft-functions/resolvers/schedulePick.js
      Resolvers:
        Query:
          getDraftPicks:
            Runtime:
              Name: APPSYNC_JS
              Version: 1.0.0
            Pipeline:
              - getDraftPicksFunction
          getDraftState:
            Runtime:
              Name: APPSYNC_JS
              Version: 1.0.0
            Pipeline:
              - getDraftStateFunction
        Mutation:
          postDraftPick:
            Runtime:
              Name: APPSYNC_JS
              Version: 1.0.0
            Pipeline:
              - postDraftPickFunction
              - schedulePickFunction
          startDraft:
            Runtime:
              Name: APPSYNC_JS
              Version: 1.0.0
            Pipeline:
              - startDraftFunction
              - schedulePickFunction
          autoPickDraftPlayer:
            Runtime:
              Name: APPSYNC_JS
              Version: 1.0.0
            Pipeline:
              - autoPickDraftPlayerFunction
              - schedulePickFunction
          endDraft:
            Runtime:
              Name: APPSYNC_JS
              Version: 1.0.0
            Pipeline:
              - endDraftFunction

  # ----------------------------------------------------------
  # Draft Functions
  # ----------------------------------------------------------
  DraftFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 15
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: '{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}'
          DB_USER: '{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}'
          DB_PASSWORD: '{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}'
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: draft-functions/
      Handler: app.lambdaHandler
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
          - '@aws-sdk/*'
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # SQS Queue for delayed autopick messages (non-VPC, no endpoint needed)
  # ----------------------------------------------------------
  AutoPickQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: autopick-queue
      VisibilityTimeout: 30

  # ----------------------------------------------------------
  # AutoPick Invoker Function (triggered by SQS)
  # ----------------------------------------------------------
  AutoPickInvokerFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 15
      Environment:
        Variables:
          APPSYNC_ENDPOINT: !GetAtt DraftAPI.GraphQLUrl
      CodeUri: autopick-invoker/
      Handler: app.lambdaHandler
      Events:
        AutoPickSQS:
          Type: SQS
          Properties:
            Queue: !GetAtt AutoPickQueue.Arn
            BatchSize: 1
      Policies:
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - appsync:GraphQL
              Resource:
                - !Sub '${DraftAPI.Arn}/types/Mutation/fields/autoPickDraftPlayer'
                - !Sub '${DraftAPI.Arn}/types/Mutation/fields/endDraft'
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - '@aws-sdk/*'
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # Draft Scheduler Function (non-VPC, sends delayed SQS messages)
  # ----------------------------------------------------------
  DraftSchedulerFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 15
      Environment:
        Variables:
          AUTOPICK_QUEUE_URL: !Ref AutoPickQueue
      CodeUri: draft-scheduler-function/
      Handler: app.lambdaHandler
      Policies:
        - SQSSendMessagePolicy:
            QueueName: !GetAtt AutoPickQueue.QueueName
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - '@aws-sdk/*'
        EntryPoints:
          - app.ts

  DraftRestFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: draft-functions/
      Handler: rest-app.lambdaHandler
      Events:
        GetDraftPlayers:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /draft/players
            Method: get
        GetAvailablePlayers:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /draft/players/available
            Method: get
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - rest-app.ts

  MatchScoringFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 15
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
          MATCH_EVENTS_QUEUE_URL: !Ref MatchEventsQueue
          MATCHUP_RESOLUTION_QUEUE_URL: !Ref MatchupResolutionQueue
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: match-scoring-functions/
      Handler: app.lambdaHandler
      Policies:
        - SQSSendMessagePolicy:
            QueueName: !GetAtt MatchEventsQueue.QueueName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt MatchupResolutionQueue.QueueName
      Events:
        CreateMatch:
          Type: Api # More info about API Event Source: https://github.com/awslabs/serverless-application-model/blob/master/versions/2016-10-31.md#api
          Properties:
            RestApiId: !Ref MyApi
            Path: /scoring/createMatch
            Method: post
        GetMatchScoring:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /scoring/{matchId}
            Method: get
        StartMatchScoring:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /scoring/{matchId}/startScoring
            Method: patch
        AddEvent:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /scoring/{matchId}/addEvent
            Method: post
        UndoEvent:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /scoring/{matchId}/undoEvent
            Method: post
        EndMatch:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /scoring/{matchId}/endMatch
            Method: patch
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
          - '@aws-sdk/*'
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # SQS Queue for match event publishing (VPC Lambda -> SQS -> non-VPC Lambda -> AppSync Events)
  # ----------------------------------------------------------
  MatchEventsQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: match-events-queue
      VisibilityTimeout: 30

  # ----------------------------------------------------------
  # Event Publisher Function (non-VPC, triggered by SQS, publishes to AppSync Events)
  # ----------------------------------------------------------
  EventPublisherFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 15
      Environment:
        Variables:
          APPSYNC_EVENTS_ENDPOINT: !Sub "https://${MatchEventsApi.Dns.Http}"
      CodeUri: event-publisher/
      Handler: app.lambdaHandler
      Events:
        MatchEventsSQS:
          Type: SQS
          Properties:
            Queue: !GetAtt MatchEventsQueue.Arn
            BatchSize: 1
      Policies:
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - appsync:EventPublish
              Resource:
                - !Sub '${MatchEventsApi.ApiArn}/channelNamespace/default'
                - !Sub '${MatchEventsApi.ApiArn}/channelNamespace/default/*'
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - '@aws-sdk/*'
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # SQS Queue for matchup resolution (triggered when an IRL match ends)
  # ----------------------------------------------------------
  MatchupResolutionQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: matchup-resolution-queue
      VisibilityTimeout: 60

  TradeExecutionQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: trade-execution-queue
      VisibilityTimeout: 60

  # ----------------------------------------------------------
  # Matchup Resolver Function (VPC, triggered by SQS, resolves fantasy matchup winners)
  # ----------------------------------------------------------
  MatchupResolverFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 30
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: matchup-resolver/
      Handler: app.lambdaHandler
      Events:
        MatchupResolutionSQS:
          Type: SQS
          Properties:
            Queue: !GetAtt MatchupResolutionQueue.Arn
            BatchSize: 1
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # Trade Executor Function (VPC, triggered by SQS, applies roster swap after veto window)
  # ----------------------------------------------------------
  TradeExecutorFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      Timeout: 30
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
      CodeUri: trade-executor-function/
      Handler: app.lambdaHandler
      Events:
        TradeExecutionSQS:
          Type: SQS
          Properties:
            Queue: !GetAtt TradeExecutionQueue.Arn
            BatchSize: 1
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # User Functions
  # ----------------------------------------------------------

  UserDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: users-functions/
      Handler: app.lambdaHandler
      Events:
        CreateUser:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /users/auth/signup
            Method: put
        GetUserProfile:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /users/profile
            Method: get
        UpdateUserProfile:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /users/profile
            Method: patch
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts
  # ----------------------------------------------------------
  # Account Functions (replaces UserDataFunction endpoints)
  # ----------------------------------------------------------

  AccountDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: account-functions/
      Handler: app.lambdaHandler
      Events:
        AccountSignup:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /account/signup
            Method: put
        GetAccountProfile:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /account/profile
            Method: get
        UpdateAccountProfile:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /account/profile
            Method: patch
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # Tournament Functions
  # ----------------------------------------------------------
  TournamentDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: tournaments-functions/
      Handler: app.lambdaHandler
      Events:
        GetAllTournaments:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /tournaments
            Method: get
        GetTournament:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /tournaments/{tournamentId}
            Method: get
        GetTournamentSeasons:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /tournaments/{tournamentId}/seasons
            Method: get
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts
  # ----------------------------------------------------------
  # Season Functions
  # ----------------------------------------------------------
  SeasonsDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: seasons-functions/
      Handler: app.lambdaHandler
      Events:
        GetSeason:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /seasons/{seasonId}
            Method: get
        GetPlayerStats:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /players/{playerId}/stats
            Method: get
        GetPlayerPerformances:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /players/{playerId}/performances
            Method: get
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts
  # ----------------------------------------------------------
  # Match Functions
  # ----------------------------------------------------------
  MatchDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: matches-functions/
      Handler: app.lambdaHandler
      Events:
        GetHomeFeedMatches:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /matches/feed
            Method: get
        GetActiveMatches:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /matches/active
            Method: get
        GetMatch:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /matches/{matchId}
            Method: get
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts
  # ----------------------------------------------------------
  # Fantasy Team Functions
  # ----------------------------------------------------------
  FantasyTeamsDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: fantasy-teams-functions/
      Handler: app.lambdaHandler
      Events:
        GetUsersFantasyTeams:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-teams/user
            Method: get
        GetFantasyTeamsInLeague:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-teams
            Method: get
        GetFantasyTeam:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-teams/{fantasyTeamId}
            Method: get
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts
  # ----------------------------------------------------------
  # Fantasy Team Instance Functions
  # ----------------------------------------------------------
  FantasyTeamInstanceDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: fantasy-team-instance-functions/
      Handler: app.lambdaHandler
      Events:
        GetFantasyTeamInstance:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-team-instance
            Method: get
        GetFantasyTeamInstancePerformances:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-team-instance/{id}/performances
            Method: get
        GetAllFantasyTeamInstances:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-team-instance/{id}/all
            Method: get
        SwapSlots:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-team-instance/{id}/swap-slots
            Method: post
        UpdateCaptains:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /fantasy-team-instance/{id}/captains
            Method: patch
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts
  # ----------------------------------------------------------
  # League Functions
  # ----------------------------------------------------------
  LeagueDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: league-functions/
      Handler: app.lambdaHandler
      Events:
        GetUserLeagues:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues
            Method: get
        GetLeagueById:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/{leagueId}
            Method: get
        GetLeagueByJoinCode:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/join/{joinCode}
            Method: get
        JoinLeagueByJoinCode:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/join/{joinCode}
            Method: post
        GetDefaultScoringRules:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/scoring-rules
            Method: get
        GetScoringRules:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/{leagueId}/scoring-rules
            Method: get
        CreateLeague:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues
            Method: post
        UpdateDraftSettings:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/{leagueId}/draft-settings
            Method: put
        UpdateScoringRules:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/{leagueId}/scoring-rules
            Method: put
        SetDraftOrder:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/{leagueId}/draft-order
            Method: put
        GetPositionRules:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /leagues/{leagueId}/position-rules
            Method: get
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts


  # ----------------------------------------------------------
  # Matchup Functions
  # ----------------------------------------------------------

  MatchupDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: matchup-functions/
      Handler: app.lambdaHandler
      Events:
        GetMatchupById:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /matchups/{matchUpId}
            Method: get
        GetMatchUpFeed:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /matchups/feed
            Method: get
        GetMatchupsByLeagueWeek:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /matchups/league/{leagueId}/week/{matchNum}
            Method: get
    Metadata:
      # Manage esbuild properties
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts

  # ----------------------------------------------------------
  # Trade Functions
  # ----------------------------------------------------------
  # ----------------------------------------------------------
  # Trade Functions
  # ----------------------------------------------------------
  TradeDataFunction:
    Type: AWS::Serverless::Function
    Properties:
      Runtime: nodejs22.x
      Architectures:
        - x86_64
      VpcConfig:
        SecurityGroupIds:
          - !Ref LambdaSecurityGroupId
        SubnetIds:
          - !Ref PrivateSubnetOneId
          - !Ref PrivateSubnetTwoId
          - !Ref PrivateSubnetThreeId
          - !Ref PrivateSubnetFourId
      Environment:
        Variables:
          DB_HOST: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:host}}"
          DB_USER: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:username}}"
          DB_PASSWORD: "{{resolve:secretsmanager:sixers-app/db-creds:SecretString:password}}"
          DB_PORT: 5432
          DB_NAME: !Ref DbName
          TRADE_EXECUTION_QUEUE_URL: !Ref TradeExecutionQueue
          TRADE_DELAY_SECONDS: "60"
      Policies:
        - SQSSendMessagePolicy:
            QueueName: !GetAtt TradeExecutionQueue.QueueName
      Layers:
        - !Ref RdsCertLayer
        - !Ref SharedUtilsLayer
      CodeUri: trade-functions/
      Handler: app.lambdaHandler
      Events:
        ProposeTrade:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/propose
            Method: post
        GetTradesList:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/list/{teamId}
            Method: get
        GetLeagueTrades:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/league/{leagueId}
            Method: get
        AcceptTrade:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/{tradeId}/accept
            Method: post
        DeclineTrade:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/{tradeId}/decline
            Method: patch
        CancelTrade:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/{tradeId}/cancel
            Method: patch
        VetoTrade:
          Type: Api
          Properties:
            RestApiId: !Ref MyApi
            Path: /trades/{tradeId}/veto
            Method: post
    Metadata:
      BuildMethod: esbuild
      BuildProperties:
        Minify: true
        Target: es2020
        Sourcemap: true
        External:
          - /opt/nodejs/*
        EntryPoints:
          - app.ts